<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.matias.pizza">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        //Definition de l'activité de démarrage par défaut (ici PizzeriaMainActivity)
        <activity
            android:name=".PizzeriaMainActivity"
            android:label="@string/app_name">

        </activity>

        //Activité PizzeriaMainActivity2
        <activity android:name=".PizzeriaMainActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity
            >
    </application>

    //Demandes d'accés internet
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    A

</manifest>